{
  "options": {
    /*
      This file allow you to permanently set options you use frequently.
      You can suspend the settings in here using the option --no-config.

      Only define options you use frequently. Any other options below should be deleted to
      allow them being used from the command line (these help entries should be deleted as well)

      The options below are the same as in the usage except the --no-* options which  are only * (use inverted boolean),
      and in camel-type instead of dash (ie. '--no-colors' becomes 'colors', '--case-sensitive' becomes 'caseSensitive' etc.)
      Remember to rename the file to .config.json and place it in user root / .mdncomp folder (see option --configpath)
    */

    "colors"        : true,     // if false, all ANSI color codes are removed
    "notes"         : true,     // show notes (if any)
    "shorthand"     : false,    // show a compressed output table
    "split"         : false,    // if shorthand, split desktop, mobile, extended at separate lines
    "caseSensitive" : false,    // use case-sensitive searches
    "desktop"       : true,     // show desktop browsers
    "mobile"        : true,     // show mobile browsers
    "ext"           : false,    // show extended status (nodejs, less common browsers)
    "overwrite"     : false,    // for --out, don't prompt but overwrite file if already exists
    "fuzzy"         : false,    // interprets keyword as fuzzy term
    "maxChars"      : 84,       // line-break at this position (except URLs)
    "doc"           : false,    // fetch, cache and show documentation (if URL for it exist)
    "docforce"      : false,    // force re-download and cache doc (to update cache)
    "desc"          : false,    // show a short description for the feature
    "specs"         : false,    // show specification links for this feature
    "waitkey"       : false,    // wait for a key press to continue (exit)
    "workers"       : false,    // show worker support information for a feature (if any)
    "sab"           : false     // show SharedArrayBuffer information for a feature (if any)
  },
  "formatter": {
    "long": {
      "sepChar": "|"            // table cell separation char in ANSI long format output
    }
  }
}
